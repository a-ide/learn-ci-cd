name: Bash tracing
on: push
jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - run: |
          set -x
          date
          hostname
      - run: |
          echo "::group::Show environment variables"
          printenv
          echo "::endgroup::"
      - run: echo "::add-mask${PASSWORD}::"
      - run: echo "${PASSWORD}"
